import React from 'react';
import { Container } from './styles';

const Warning = () => {
	return (
		<Container>
			<span><b>E X A M P L E </b> Only a example for a warning, if you like donate $100 (USA ONLY) </span>
		</Container>
	);
};

export default Warning;